-- calculate fibonacci numbers
fib = {n |
      	 prevPrevTerm = 0
	 prevTerm = 1
	 i = 1
	 while i < n
	   tmp = prevTerm
  	   prevTerm = prevPrevTerm + prevTerm
  	   prevPrevTerm = tmp
  	   i = i + 1
	 end
	 return prevTerm
      }

